# vim:ft=python

obj = bld.new_task_gen(
        features = 'cc cprogram',
        source = 'main.c',
        target = 'rss2mail',
        includes = '.')

obj = bld.new_task_gen(
        feature = 'cc cprogram',
        source = 'parser.c',
        target = 'parser',
        uselib = 'LIBXML')

obj = bld.new_task_gen(
        feature = 'cc cprogram',
        source = 'rss2mail.c',
        target = 'rss2mail',
        uselib = 'LIBCURL LIBXML')
